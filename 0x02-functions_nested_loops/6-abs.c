#include "main.h"

/**
* _abs - computes the absolute value of an integer.
*
* @n: number to check
*
* Return : returns n if less or greater than 0
*/

int _abs(int n)
{
	if (n < 0 || n > 0)
		return (n);
	else
		return (0);
}
