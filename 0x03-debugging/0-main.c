#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
* main - a program that prints a random number
*
* Return: 0
*/
int main(void)
{
	int n;

	n = 0;
	/* code by SammieDevs*/
	if (n > 0)
		printf("%d is positive\n", n);
	else if (n < 0)
		printf("%d is negative\n", n);
	else
		printf("%d is zero\n", n);
	return (0);
}
